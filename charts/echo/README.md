# echo Helm Chart

This chart deploys the simple `http-echo` application.

Install (example):

```bash
helm install my-echo charts/echo --namespace default --create-namespace
```

Customize values in `charts/echo/values.yaml`.
